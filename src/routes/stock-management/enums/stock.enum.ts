export enum EStockMovementType {
  RECEIVE = 'RECEIVE',
  ISSUE = 'ISSUE',
  ADJUST = 'ADJUST',
  RESERVE = 'RESERVE',
  RELEASE = 'RELEASE',
}

export enum EStockStatus {
  OUT = 'out',
  LOW = 'low',
  NORMAL = 'normal',
}